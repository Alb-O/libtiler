[package]
name = "libtiler"
version = "0.1.0"
edition = "2024"
description = "Deterministic binary split tiling library with certified integer layout solving"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
proptest = "1.6"
serde_json = "1.0"
